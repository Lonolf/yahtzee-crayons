export const user = {
  userId: null,
  userName: null,
  userEmail: null,
  userRole: null,
}

export const settings = {
  players: 2,
  sets: 3,
  maxThrows: 3,
  virtualDices: true,
}

export const loading = ['autoLogin']
